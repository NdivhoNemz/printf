#include "main.h"

/**
 * character_count - a function returns number of characters printed.
 * Return: The number of characters printed.
 */

int character_count(void)
{
	int num_of_characters = 0; /* external variable initialize */

	return (num_of_characters);
}
